namespace ProjectPortfolio2026.ResumeParser;

/// <summary>
/// Marks the dedicated resume parser assembly so future parser components
/// can anchor reflection, DI registration, or tests without depending on
/// unrelated server types.
/// </summary>
public static class ResumeParserAssemblyMarker
{
}
